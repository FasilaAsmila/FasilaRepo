var isLoggedIn = true;
var isEmailVerified = false;
var cardValid = true;

if(isLoggedIn || isEmailVerified || cardValid)
{
    console.log("proceed to do purchase");
}else{
    console.log("ur nt alovd to do purchase");
}